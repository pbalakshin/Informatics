\documentclass[14pt, fleqn]{extarticle}  % 14pt — требование ГОСТ для основного текста

% =============================================================================
% ГЕОМЕТРИЯ СТРАНИЦЫ
% =============================================================================
% Поля по ГОСТ 2.105-2019: левое 30мм, правое 10мм, верхнее/нижнее 20мм
% Здесь 2.5см со всех сторон — отклонение от ГОСТ (требуется исправить)
\usepackage[
    left=2.5cm,
    right=2.5cm,
    top=2.5cm,
    bottom=2.5cm,
    marginparwidth=2.5cm,
    marginparsep=5mm
]{geometry}

% =============================================================================
% БАЗОВЫЕ ПАКЕТЫ
% =============================================================================
\usepackage{xparse} % Для гибких аргументов окружений

% =============================================================================
% ШРИФТЫ И ЯЗЫК
% =============================================================================
% T2A — кириллическая кодировка, требование ГОСТ для русских текстов
\usepackage[T1,T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{textcomp} % Спецсимволы ©, ®, ™
\usepackage[english,russian]{babel}

% Шрифт Times в тексте как основной (аналог Times New Roman по ГОСТ)
\usepackage{tempora}
% Шрифт Times в формулах как основной (ГОСТ требует единый шрифт для формул)
\usepackage[varg, cmbraces, cmintegrals]{newtxmath}
% =============================================================================
% ТИПОГРАФИКА
% =============================================================================
% fvextra должен быть загружен до csquotes (для подавления предупреждения)
\usepackage{fvextra}
\usepackage[autostyle, style=russian, babel]{csquotes}  % Русские кавычки-ёлочки по ГОСТ
\usepackage{indentfirst}  % Разрешает отступ у первого абзаца (требование ГОСТ)
%% Абзацный отступ по ГОСТ 2.105-2019 — 1.25 см (пять букв)
\setlength{\parindent}{1.25cm}  % ✅ РЕАЛИЗОВАНО — требование ГОСТ 2.105-2019, 5.1.4

\usepackage{setspace}
\onehalfspacing%  % Полуторный интервал по ГОСТ 2.105-2019

\usepackage{microtype}

\usepackage{ulem} % Зачёркнутый текст
\usepackage{xspace}

% =============================================================================
% ЦВЕТА (должен быть до tikz/pgfplots)
% =============================================================================
\usepackage[table]{xcolor}
% draw.io cell colors
\definecolor{iogreen}{RGB}{213 232 212}
\definecolor{ioyellow}{RGB}{255, 242, 204}
\definecolor{ioorange}{RGB}{255, 230, 204}
\definecolor{iored}{RGB}{248, 206, 204}

% =============================================================================
% ГРАФИКА
% =============================================================================
\usepackage{graphicx}
\usepackage{wrapfig}  % Обтекание рисунков текстом (допускается ГОСТ)
% \usepackage{svg}  % Не используется - графика конвертируется в PDF

% =============================================================================
% TIKZ И PGFPLOTS (должны быть после xcolor)
% =============================================================================
\usepackage{tikz}
\usetikzlibrary{graphs}
\usepackage{pgfplots}
\pgfplotsset{compat=newest}

% =============================================================================
% МАТЕМАТИКА
% =============================================================================
\usepackage{amsmath}  % Расширенные математические возможности (требуется ГОСТ для формул)
\usepackage{xfrac}  % Красивые дроби
\input{preamble/siunitx.tex}  % Единицы измерения по ГОСТ

% Использовать полужирное начертание для векторов (соответствует ГОСТ для обозначений)
\let\vec=\mathbf%

% =============================================================================
% ПОДПИСИ К РИСУНКАМ И ТАБЛИЦАМ
% =============================================================================
% ГОСТ 2.105: подпись рисунка снизу, подпись таблицы сверху
\usepackage[
    font=normalsize,
    labelfont=normalsize,
    textfont=it,
    singlelinecheck=false,
    labelsep=endash,
    position=top,
    justification=justified
]{caption}
\captionsetup[figure]{position=bottom,justification=centering}  % Подпись рисунка снизу — по ГОСТ
\usepackage{subcaption}

\usepackage{float}  % Позволяет фиксировать положение рисунков (допускается ГОСТ)

% =============================================================================
% ЛИСТИНГИ КОДА
% =============================================================================
% ГОСТ не регламентирует оформление кода, minted предоставляет качественную вёрстку
\usepackage[cache=true]{minted}

\setminted{
    fontsize=\small,
    frame=single,
    encoding=utf8,
    samepage,
    autogobble,
    tabsize=4
}

\setmintedinline{
    fontsize=\normalsize,
    encoding=utf8,
    breaklines,
    breakafter=-/
}

% Отключение курсива в minted
\usepackage{xpatch}
\xpatchcmd{\mint}{\begingroup}{\begingroup\let\itshape\relax}{}{} % chktex 6
\xpatchcmd{\inputminted}{\begingroup}{\begingroup\let\itshape\relax}{}{} % chktex 6
\xpatchcmd{\mintinline}{\begingroup}{\begingroup\let\itshape\relax}{}{} % chktex 6
\xpatchcmd{\minted}{\VerbatimEnvironment}{\VerbatimEnvironment\let\itshape\relax}{}{} % chktex 6

% =============================================================================
% ТАБЛИЦЫ
% =============================================================================
% ГОСТ 2.105: таблицы должны иметь заголовок сверху, допускается перенос на след. страницу
\usepackage{longtable}  % Многостраничные таблицы
\usepackage{tabularx}
\usepackage{xltabular}  % Комбинация longtable и tabularx
\usepackage{hhline}
\usepackage{booktabs}  % Качественные линии таблиц (не по ГОСТ, но улучшает читаемость)
\usepackage{array}
\usepackage{makecell}
\usepackage{multirow}

\renewcommand\theadfont{\normalsize\bfseries}  % Жирный заголовок таблицы — по ГОСТ

\newcolumntype{C}{>{\centering\arraybackslash}X}
\newcolumntype{L}{>{\raggedright\arraybackslash}X}
\newcolumntype{R}{>{\raggedleft\arraybackslash}X}

\renewcommand\tabularxcolumn[1]{m{#1}}% for vertical centering text in X column

\usepackage{tabularray}

% =============================================================================
% СПИСКИ И ПЕРЕЧНИ
% =============================================================================
% ГОСТ 2.105: маркированные списки с тире, нумерованные строчными буквами с закрывающей скобкой
\usepackage{enumitem}
\input{preamble/enumitem.tex}  % Настройки списков по ГОСТ

% =============================================================================
% СНОСКИ
% =============================================================================
% ГОСТ 2.105: сноски оформляются постранично (perpage)
\usepackage[perpage]{footmisc}

% =============================================================================
% ФОРМАТИРОВАНИЕ СЕКЦИЙ
% =============================================================================
% ГОСТ 2.105: заголовки разделов выравниваются по центру, без точки в конце
\usepackage{titlesec}
\titleformat*{\section}{\large\bfseries}  % Заголовки разделов — крупный жирный
\titleformat*{\subsection}{\normalsize\bfseries}  % Заголовки подразделов — обычный жирный
\titleformat*{\subsubsection}{\normalsize\bfseries}  % Заголовки подподразделов — обычный жирный
% \titleformat*{\paragraph}{\normalsize\bfseries}

\titleformat{\paragraph}
{\normalfont\normalsize\bfseries}{\theparagraph}{1em}{}
\titlespacing*{\paragraph}{0pt}{3.25ex plus 1ex minus .2ex}{1.5ex plus .2ex}
\titleformat*{\subparagraph}{\normalsize\bfseries}

% =============================================================================
% TODO ЗАМЕТКИ
% =============================================================================
% todonotes не соответствует ГОСТ, используется только для черновика
% Перед финальной сборкой все заметки должны быть убраны
\usepackage[colorinlistoftodos]{todonotes}
\presetkeys{todonotes}{inline,backgroundcolor=yellow}{}
%% Закомментировать при дебаге и раскомментировать при релизе
%% наличие данной команды убирает все блоки todo с документа
% \presetkeys{todonotes}{disable}{}  % Раскомментировать для финальной версии!

% =============================================================================
% ССЫЛКИ (должны быть почти последними)
% =============================================================================
% ГОСТ 7.0.5-2008: гиперссылки допускаются в электронных версиях
\usepackage[breaklinks]{hyperref}
\hypersetup{
    colorlinks,  % Цветные ссылки (для электронной версии)
    linktoc=page,
    pdflang={ru}
}

\usepackage{xurl}
\usepackage{xr} % cross-file references
\usepackage{lastpage}
\input{preamble/cleveref.tex}

% =============================================================================
% НАСТРОЙКА ТЕХОВСКИХ ШТРАФОВ (для формирования абзацев)
% =============================================================================
% ГОСТ 2.105: не допускается перенос слов в заголовках и наличие висячих строк
\sloppy             % «Будь менее строгим к выравниванию строк»
\binoppenalty=10000 % Запрещает разрыв строки после бинарного оператора
\relpenalty=10000   % Запрещает разрыв строки после знаков отношений
\clubpenalty=10000  % Запрещает висячие строки в начале абзаца (требование ГОСТ)
\widowpenalty=10000 % Запрещает висячие строки в конце абзаца (требование ГОСТ)

% =============================================================================
% БИБЛИОГРАФИЯ (biblatex + biber)
% =============================================================================
% ГОСТ 7.1-2003, ГОСТ 7.0.5-2008: библиография оформляется по ГОСТ
\input{preamble/bibliography}
\addbibresource{bibliography.bib}
\providecommand\phantomsection{}

% =============================================================================
% ОКРУЖЕНИЕ EXAMPLE С АВТОМАТИЧЕСКОЙ НУМЕРАЦИЕЙ
% =============================================================================
\newcounter{examplenum}[subsubsection]

% Окружение example (с отступом сверху)
\newenvironment{example}
{%
    \refstepcounter{examplenum}%
    \begin{list}{}{%
        \setlength{\labelwidth}{2cm}%
        \setlength{\leftmargin}{2.5cm}%
        \setlength{\labelsep}{0.5cm}%
        \setlength{\itemsep}{2pt}%
        \setlength{\parsep}{0pt}%
        \renewcommand{\makelabel}[1]{\hfill\textit{##1}}%
    }%
    \item[\fontseries{bx}\fontshape{n}\selectfont\uline{Пример~\theexamplenum:}]
}{%
    \end{list}%
}

% Команды для использования внутри окружения example
\NewDocumentCommand{\task}{}{\item[\uline{Задание:}]}
\NewDocumentCommand{\solution}{}{\item[\uline{Решение:}]}
\NewDocumentCommand{\answer}{}{\item[\uline{Ответ:}]}

% Inline-команда для примеров внутри текста
\newcommand{\ex}{%
    \refstepcounter{examplenum}%
    \textbf{Пример~\theexamplenum:}\xspace
}

% =============================================================================
% ОКРУЖЕНИЕ "ГДЕ" ДЛЯ ФОРМУЛ
% =============================================================================
% ГОСТ 2.105: после формулы следует пояснение значений символов со слова "где"
\newenvironment{where}
  {\par\nobreak\noindent\textit{где}\ %
   \begin{list}{}
     {\setlength{\labelwidth}{0pt}
      \setlength{\leftmargin}{2em}
      \setlength{\itemsep}{0pt}    % Между пунктами
      \setlength{\parsep}{0pt}     % Между абзацами
      \setlength{\topsep}{2pt}     % Отступ сверху (было ~6pt по умолчанию)
      \setlength{\partopsep}{0pt}  % Доп. отступ если новый параграф
      \renewcommand{\makelabel}[1]{##1\hfill}}}
  {\end{list}}

\newcommand{\hiddensection}[1]{
    \phantomsection%
    \section*{#1}
    \addcontentsline{toc}{section}{#1}
}

\newcommand{\hiddensubsection}[1]{
    \phantomsection%
    \subsection*{#1}
    \addcontentsline{toc}{subsection}{#1}
}

\newcommand{\hiddensubsubsection}[1]{
    \phantomsection%
    \subsubsection*{#1}
    \addcontentsline{toc}{subsubsection}{#1}
}

% =============================================================================
% Различные хуки
% =============================================================================
\AddToHook{env/tblr/before}{\vspace{2ex}}
\AddToHook{env/tblr/after}{\vspace{2ex}}

\AddToHook{cmd/section/before}{\clearpage}
\AddToHook{cmd/section*/before}{\clearpage}

% =============================================================================
% ПУТЬ ДЛЯ КАРТИНОК
% =============================================================================
\graphicspath{ {./images/} }

% =============================================================================
% НУМЕРАЦИЯ СТРАНИЦ
% =============================================================================
% ГОСТ 7.32-2017: сквозная нумерация страниц арабскими цифрами
\pagestyle{plain}
\setcounter{page}{2}

% =============================================================================
% ДОКУМЕНТ
% =============================================================================
\begin{document}
\nocite{*}

% \todo{Если видно это сообщение - раскомментировать строку в main.tex}
\input{0_title_page} % обложка, не идёт в счёт страниц
\input{1_first_page}
\input{2_description}

% Вставка оглавления, начиная с 3 страницы
\setcounter{page}{3}
\addtocontents{toc}{\protect\sloppy}
\tableofcontents

% TODO: remove
\todototoc%
\clearpage
\listoftodos%

\input{3_introduction}
\input{src/00_about_the_course}
\input{src/01_theory}
\input{src/02_data_measurement_units}
\input{src/03_notation_systems}
\input{src/04_limited_arithmetics}
\input{src/05_data_compression}
\input{src/06_error_correcting_coding}
\input{src/07_boolean_algebra}
\input{src/08_software}
\input{src/09_machine}
\input{src/10_memory}
\input{src/11_osi}

\input{4_conclusion}

\addbibliography{Список использованных и рекомендованных источников}

\input{5_last_page}
\input{6_back_page} %задняя страница обложки, не идёт в счёт страниц
\end{document} % chktex 17
